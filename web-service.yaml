apiVersion: v1
kind: Service
metadata:
  name: mlops-tumor-web-service
spec:
  selector:
    app: mlops-tumor-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501
  type: LoadBalancer